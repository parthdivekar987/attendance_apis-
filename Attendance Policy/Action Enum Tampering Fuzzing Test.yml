info:
  name: Action Enum Tampering Fuzzing Test
  type: http
  seq: 9

http:
  method: PATCH
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendancepolicy/20/status?action=TAMPERED_ACTION_PROBE
  headers:
    - name: Authorization
      value: Bearer{{authToken}}
    - name: Content-Type
      value: application/json
  params:
    - name: action
      value: TAMPERED_ACTION_PROBE
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
