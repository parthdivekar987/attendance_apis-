info:
  name: Duplicate Policy Name Collision Defect
  type: http
  seq: 7

http:
  method: POST
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendancepolicy
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "policyName": "LeaveAttendance",
        "policyHeading": "Leave and Attendance Rule",
        "policyDescription": "Testing duplicate policy name creation without uniqueness gating."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
