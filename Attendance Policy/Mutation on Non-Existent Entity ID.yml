info:
  name: " Mutation on Non-Existent Entity ID"
  type: http
  seq: 10

http:
  method: PUT
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendancepolicy/999999
  headers:
    - name: Authorization
      value: bearer{{authToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "policyName": "Ghost Entity Update",
        "policyHeading": "Non Existent Policy Record",
        "policyDescription": "Testing entity lookup resilience on non-existent policy identifier."
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
