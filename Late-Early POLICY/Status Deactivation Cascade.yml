info:
  name: Status Deactivation Cascade
  type: http
  seq: 13

http:
  method: PATCH
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/138/status?status=N
  headers:
    - name: Authorization
      value: bearer {{authToken}}
    - name: ciontent-type
      value: application/json
  params:
    - name: status
      value: N
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
