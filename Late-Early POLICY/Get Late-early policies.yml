info:
  name: Get Late/early policies
  type: http
  seq: 2

http:
  method: GET
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/getById/152
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
