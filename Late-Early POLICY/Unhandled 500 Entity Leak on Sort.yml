info:
  name: Unhandled 500 Entity Leak on Sort
  type: http
  seq: 6

http:
  method: GET
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendance/late-early-policies/getAll?page=0&size=10&sort=non_existent_column,desc
  headers:
    - name: Authorization
      value: Bearer{{authToken}}
    - name: Content-Type
      value: application/json
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "10"
      type: query
    - name: sort
      value: non_existent_column,desc
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
