info:
  name: "TOCTOU "
  type: http
  seq: 2

http:
  method: POST
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendancepolicy
  headers:
    - name: Authorization
      value: Bearer{{authToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "policyName": "ConcurrentRacePolicyMXIACC",
        "policyHeading": "Concurrency Race Test",
        "policyDescription": "Testing simultaneous creation race condition"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
