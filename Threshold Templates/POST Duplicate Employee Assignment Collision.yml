info:
  name: POST Duplicate Employee Assignment Collision
  type: http
  seq: 12

http:
  method: POST
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/create
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "thresholdCode": "TH_DUP_EMP_TEST",
        "thresholdName": "Duplicate Employee Assignment Test",
        "description": "Testing duplicate employee in same assignment array",
        "templateMode": "CUSTOM",
        "shiftTypeApplicability": "Fixed",
        "absentMaxHours": 3.00,
        "halfDayMinHours": 3.01,
        "fullDayMinHours": 6.00,
        "presentMinHours": 8.00,
        "effectiveFrom": "2035-01-01",
        "effectiveTo": "2035-12-31",
        "isDefault": "N",
        "isActive": "Y",
        "templateStatus": "DRAFT",
        "remarks": "Duplicate employee bug verification",
        "assignments": [
          {
            "employeeId": 3701,
            "effectiveFrom": "2035-01-01",
            "effectiveTo": "2035-12-31",
            "isActive": "Y"
          },
          {
            "employeeId": 3701,
            "effectiveFrom": "2035-01-01",
            "effectiveTo": "2035-12-31",
            "isActive": "Y"
          }
        ]
      }
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        console.log("Status:", res.getStatus());
        console.log("Response:", JSON.stringify(res.getBody(), null, 2));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5