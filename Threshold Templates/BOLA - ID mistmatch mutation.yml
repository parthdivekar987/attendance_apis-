info:
  name: BOLA - ID mismatch mutation
  type: http
  seq: 15

http:
  method: PUT
  url: https://uatmcdphcmplatform.omfysgroup.com/api/attendance/status-threshold/update/80
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "thresholdId": 9999,
        "thresholdCode": "TH00079",
        "thresholdName": "BOLA ID Mismatch Test",
        "description": "Testing path parameter thresholdId 80 vs body thresholdId 9999 mismatch",
        "templateMode": "CUSTOM",
        "shiftTypeApplicability": "Fixed",
        "absentMaxHours": 3.00,
        "halfDayMinHours": 3.01,
        "fullDayMinHours": 6.00,
        "presentMinHours": 8.00,
        "effectiveFrom": "2027-01-01",
        "effectiveTo": "2027-12-31",
        "isDefault": "N",
        "isActive": "Y",
        "templateStatus": "DRAFT",
        "remarks": "BOLA security mutation test",
        "assignments": [
          {
            "employeeId": 34,
            "effectiveFrom": "2027-01-01",
            "effectiveTo": "2027-12-31",
            "isActive": "Y"
          }
        ]
      }
  auth:
    type: bearer
    token: "{{authToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        console.log("Status:", res.getStatus());
        console.log("Response:", JSON.stringify(res.getBody(), null, 2));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
